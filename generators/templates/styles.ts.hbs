import styled, { css } from 'styled-components'
import * as T from './types'

export const {{pascalCase name}}Container = styled.main<T.{{pascalCase name}}Props>`
  ${({ theme }) => css`
    background: ${theme['gray500']}  
  `}  
`