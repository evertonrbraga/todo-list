import { render, screen } from 'project-testing-library'
import { {{pascalCase name}} } from '.'

describe('<{{pascalCase name}} />', () => {
  it('should snapshot the {{pascalCase name}} component', () => {
    const { container } = render(<{{pascalCase name}} />)
    expect(container.firstChild).toMatchSnapshot()
  })

  it('should if the title is rendering correctly', () => {
    render(<{{pascalCase name}} />)
    const title = screen.getByText('{{pascalCase name}}')
    expect(title).toBeInTheDocument()
  })
})